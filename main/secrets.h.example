#pragma once

/* Copy this file to secrets.h and fill in real values.
 * secrets.h is gitignored and must never be committed. */

#define WIFI_SSID      "your-ssid"
#define WIFI_PASSWORD  "your-password"

#define MQTT_BROKER_URI "mqtt://192.168.1.10:1883"
#define MQTT_USERNAME   "activator"
#define MQTT_PASSWORD   "changeme"
